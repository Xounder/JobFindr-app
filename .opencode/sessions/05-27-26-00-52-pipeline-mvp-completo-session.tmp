DONE: Full pipeline execution (PM→TL→FE+BE→QA→loop→done). 94 tasks: apps/frontend (React 19, Zustand, TanStack Query, 14 components), apps/backend (Fastify, 14 modules: search, providers, scraping, normalization, matchmaking, trust, ranking, cache, security, observability), packages/types+utils, docker-compose.yml
WRONG: pipeline.yaml got duplicate YAML keys from partial edits — old "pending" entries remained alongside new "completed" ones, overwriting statuses. All SKILL.md + agent .md files referenced specs/plan/ but actual path is .opencode/plan/ (16 files affected). Backend package.json had no build script initially. QA found 3 dead-code files never imported.
IMPROV: Always validate pipeline.yaml after edits. Standardize path convention across all skills/agents (done). Add dead-code check to QA checklist (done). Force build script requirement in backend skill (done). Add YAML validation rule to pipeline skill (done).
LEARN: Parallel FE/BE execution works cleanly — tasks are truly independent. QA loop caught real issues (dead code, missing docker-compose, trailing space bug). Tech Lead must define canonical allocation schema (Layer:/Depends on:) before implementation starts.
NEXT: Add test framework (vitest), CI (GitHub Actions), tests for matchmaking/trust/ranking engines, and backfill missing packages/shared and packages/configs.
