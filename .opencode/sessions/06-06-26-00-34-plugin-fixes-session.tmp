DONE: Fixed session-load-autoloader plugin (event hook, not named hooks), created OPENCODE_PLUGINS.md, fixed sound-alert and validate-agent plugins (replaced invalid hooks with valid ones), created secure-access plugin (blocks external file access and undocumented commands), removed debug logs from all working plugins
WRONG: Initially relied on non-existent named hooks (session.created, file.edited), wasted time on experimental.chat.system.transform (unwired in runtime), validate-agent not yet confirmed working
IMPROV: Always check the actual Hooks interface in @opencode-ai/plugin/dist/index.d.ts before implementing, use chat.message as the first test hook, follow existing plugin patterns, add PLUGIN_INITIALIZED logs during dev and remove when stable
LEARN: OpenCode v1.15.10 only supports the generic event hook for session/file events -- named hooks like session.created or file.edited don't exist; experimental.* hooks are typed but not wired; the Hooks interface in node_modules is the source of truth; console.log output appears in the OpenCode UI; plugin runtime uses Bun with full Node.js fs compatibility
NEXT: Test validate-agent by editing pipeline.yaml, explore untested hooks (chat.params, shell.env), consider a 400-line file limit enforcement plugin