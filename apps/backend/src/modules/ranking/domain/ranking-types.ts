/**
 * Ranking domain types (re-export from shared types).
 */
export type { RankingWeights, RankingResult, RankingBreakdown } from '@jobfindr/types'
export { DEFAULT_RANKING_WEIGHTS } from '@jobfindr/types'
